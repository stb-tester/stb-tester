# We don't want to be generating a test for Dialog as it is defined in a
# separate file where it will already have tests generated for it.
from example import Dialog  # pylint: disable=unused-import
